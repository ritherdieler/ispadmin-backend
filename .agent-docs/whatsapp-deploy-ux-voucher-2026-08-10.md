# Deploy prod WhatsApp UX + voucher dedup — 2026-08-10

- **Commit:** `ea98aaf` (`develop`)
- **Release:** `1.0.3+ea98aaf`
- **Incluye:**
  - Diagnóstico stale → menú (no `UNKNOWN`)
  - Copy usted + tildes + CTA MENU
  - Dedup ACK voucher 3 min
- **Health:** `GET /ispadmin/` → HTTP 200
- **Obs:** deploy event registrado
