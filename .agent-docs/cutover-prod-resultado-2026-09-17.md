# Cutover prod ← develop — resultado 2026-09-17

**OK** segundo intento. Pin `1.0.3+978f987`. HTTP 200. Flyway V38–V55 `success=1`. `access_mode` 1349 `STATIC_IP`. `priority` INT. Login+search 200. Lab `#35`/`#36` intactos. Sin restore.

No se redeployó `56991bd`. Detalle: store `docs/cutover-prod-resultado.md`.
